# 사고 실험 · Idea Lab v0.1.1 설치/사용 가이드

이 앱은 별도 APK 설치 파일이 아니라, **웹사이트를 안드로이드 홈 화면에 앱처럼 설치하는 PWA** 방식입니다.

가장 쉬운 설치 순서:

**PC에서 GitHub에 파일 올리기 → GitHub Pages 켜기 → 안드로이드 Chrome에서 주소 열기 → 홈 화면에 설치**

---

## 0. 압축을 풀었을 때 보여야 하는 파일

폴더 안에 아래 항목이 있으면 정상입니다.

- `index.html`
- `app.js`
- `styles.css`
- `manifest.webmanifest`
- `sw.js`
- `icons` 폴더
- `README.md`
- `INSTALL_GUIDE_KO.md`
- `PRODUCT_NOTE.md`
- `serve.py`
- `.nojekyll`

**중요:** GitHub에 올릴 때는 이 바깥 폴더 자체가 아니라, 위 파일들이 보이는 **폴더 안쪽 내용물**을 올리세요.  
GitHub 저장소 첫 화면에서 `index.html`이 바로 보여야 합니다.

---

# 1. GitHub 저장소 만들기

## 1-1. GitHub 접속
브라우저에서 GitHub에 로그인합니다.

## 1-2. 새 저장소 만들기
오른쪽 위 `+` 버튼 → `New repository`를 누릅니다.

추천 설정:

- Repository name: `idea-lab`
- Description: `Personal thought experiment app`
- Visibility: `Public`

처음 테스트할 때는 Public이 가장 간단합니다.

> 앱에 기록하는 실제 아이디어 내용은 GitHub 저장소에 올라가지 않습니다.  
> 기본적으로 아이디어 데이터는 휴대폰 브라우저의 로컬 저장소에 저장됩니다.

`Create repository`를 누릅니다.

---

# 2. 앱 파일을 GitHub에 올리기

새 저장소 화면에서:

1. `Add file` → `Upload files`
2. PC에서 압축을 푼 `thought_experiment_app_v0.1.1` 폴더를 엽니다.
3. 폴더 안에서 `Ctrl + A`로 **안의 파일/폴더 전부**를 선택합니다.
4. GitHub의 업로드 화면으로 드래그합니다.
5. 업로드 목록에 `index.html`, `app.js`, `styles.css`, `icons/...` 등이 보이는지 확인합니다.
6. Commit message에 예: `Initial Idea Lab v0.1.1`
7. `Commit changes`를 누릅니다.

업로드가 끝나면 GitHub 저장소의 첫 화면에 **`index.html`이 바로 보여야 합니다.**

잘못된 예:
`thought_experiment_app_v0.1.1/index.html`

올바른 예:
`index.html`

---

# 3. GitHub Pages 켜기

저장소 화면에서:

1. 상단 `Settings`
2. 왼쪽 메뉴에서 `Pages`
3. `Build and deployment`
4. `Source` → `Deploy from a branch`
5. `Branch` → `main`
6. 폴더 → `/(root)`
7. `Save`

잠시 기다리면 GitHub Pages 주소가 생성됩니다.

보통 주소 형태:

`https://내-GitHub-아이디.github.io/idea-lab/`

`Settings → Pages` 화면에 `Visit site` 버튼이 생기면 배포 완료입니다.

처음에는 수 분 걸릴 수 있습니다.

---

# 4. PC에서 먼저 확인하기

`Visit site`를 눌렀을 때 아래가 보이면 성공입니다.

- 상단: `사고 실험`
- `1단계 · 붙잡기`
- 큰 원형 `눌러서 말하기` 버튼
- 텍스트 입력창
- `생각 저장 · 발전` 버튼
- 하단 메뉴: `기록 / 보관함 / 연결 / 설정`

---

# 5. 안드로이드 폰에 앱처럼 설치하기

## 5-1. 폰에서 주소 열기
PC에서 만든 GitHub Pages 주소를 휴대폰의 **Chrome**에서 엽니다.

예:
`https://내아이디.github.io/idea-lab/`

주소를 카카오톡/메일 등으로 자기 자신에게 보내두면 입력이 편합니다.

## 5-2. 홈 화면 설치
Chrome 오른쪽 위 `⋮` 메뉴를 누릅니다.

휴대폰/Chrome 버전에 따라 다음 중 하나가 보일 수 있습니다.

- `앱 설치`
- `홈 화면에 추가`
- `바로가기 추가`

표시되는 설치 항목을 선택합니다.

설치가 끝나면 홈 화면에 **사고 실험** 아이콘이 생깁니다.

이제 Chrome 탭이 아니라 일반 앱처럼 독립 창으로 열립니다.

---

# 6. 처음 사용하는 방법

## 첫 번째 아이디어 기록

1. 홈 화면의 `사고 실험` 앱을 엽니다.
2. `눌러서 말하기`를 누릅니다.
3. 마이크 권한 요청이 나오면 `허용`
4. 생각나는 말을 그대로 말합니다.
5. 음성 인식 결과가 입력창에 들어옵니다.
6. 필요하면 키보드로 문장을 조금 고칩니다.
7. `생각 저장 · 발전`을 누릅니다.

예:

`고래는 등이 검고 배는 흰데, 바다에서 위와 아래의 포식자 눈을 피하기 위한 것 아닐까? 다른 수중동물도 비슷한가?`

저장하면 앱이 기본적으로:

- 제목
- 요약
- 키워드
- 핵심 가설
- 반례/확장 질문
- 최소 사고 실험

을 만들어 줍니다.

---

# 7. 하단 메뉴의 뜻

## 기록
새로운 아이디어를 빠르게 붙잡는 곳.

## 보관함
그동안 저장한 아이디어를 보는 곳.

상태를 다음처럼 발전시킬 수 있습니다.

`씨앗 → 발전 중 → 실행 후보`

## 연결
아이디어가 여러 개 쌓이면 서로 비슷한 생각을 찾아 연결합니다.

## 설정
- 한국어/영어 음성 인식
- 향후 AI Webhook 연결
- 백업 가져오기
- 전체 초기화

---

# 8. 데이터는 어디에 저장되나?

v0.1.1에서는 아이디어를 별도 서버나 GitHub에 자동 전송하지 않습니다.

기본 저장 위치는 **현재 휴대폰의 Chrome/PWA 로컬 저장소**입니다.

따라서:

- GitHub에는 앱 프로그램 파일만 올라갑니다.
- 사용자가 기록한 아이디어는 GitHub에 공개되지 않습니다.
- 브라우저 데이터 삭제/휴대폰 교체 시에는 아이디어가 사라질 수 있습니다.

그래서 중요한 아이디어가 쌓이면 `보관함 → 내보내기`로 JSON 백업 파일을 가끔 저장해 두세요.

주의:
음성 받아쓰기는 Android/Chrome의 음성 인식 기능을 사용하므로, 기기 및 Chrome 설정에 따라 네트워크 음성 인식 서비스가 사용될 수 있습니다.

---

# 9. 앱을 나중에 업데이트하는 방법

v0.2 같은 새 버전을 받으면 같은 GitHub 저장소에 새 파일을 업로드해 기존 파일을 교체하면 됩니다.

기본 흐름:

1. 새 버전 압축 해제
2. GitHub `idea-lab` 저장소 열기
3. 변경된 파일 업로드
4. `Commit changes`
5. GitHub Pages가 자동 재배포
6. 휴대폰에서 앱을 다시 열기

PWA 캐시 때문에 옛 화면이 잠시 남을 경우 Chrome에서 페이지를 새로고침하거나 앱을 완전히 종료 후 다시 열면 됩니다.

---

# 10. 문제 발생 시 먼저 확인할 것

## 사이트가 404로 나올 때
- `Settings → Pages`
- Source가 `Deploy from a branch`
- Branch가 `main`
- Folder가 `/(root)`
인지 확인합니다.

그리고 저장소 첫 화면에서 `index.html`이 바로 보여야 합니다.

## 마이크가 안 될 때
- Android 설정 → 앱 → Chrome → 권한 → 마이크 허용
- Chrome에서 GitHub Pages 사이트의 마이크 권한 허용
- 반드시 `https://` 주소로 접속

## 홈 화면 설치 메뉴가 안 보일 때
- Chrome에서 사이트를 먼저 정상적으로 한 번 엽니다.
- 잠시 기다렸다가 `⋮` 메뉴를 다시 확인합니다.
- `앱 설치` 대신 `홈 화면에 추가`로 표시될 수 있습니다.

## 저장한 생각이 다른 PC/폰에서 안 보일 때
정상입니다. v0.1.1은 계정 동기화가 아니라 **기기별 로컬 저장** 방식입니다.

---

# 가장 먼저 해볼 테스트

설치 후 아래 한 문장만 음성으로 넣어 보세요.

`고래의 몸 색깔은 위와 아래에서 오는 포식자의 시선을 피하도록 진화한 게 아닐까?`

그리고 `생각 저장 · 발전`을 누른 뒤,
보관함과 연결 탭이 어떻게 작동하는지 확인하면 됩니다.
